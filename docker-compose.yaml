# vim: set ft=dosini
---
version: "3"
services:
  letsencrypt:
    image: linuxserver/letsencrypt
    container_name: le
    ports:
      - 443:443
    volumes:
      - /opt/appdata/letsencrypt:/config
    restart: always

    environment:
      - PUID=${PUID}
      - PGID=${PGID}
      - EMAIL=collinsbiegon9@gmail.com
      - URL=cyphercore.dev
      - SUBDOMAINS=www,biegon
      - ONLY_SUBDOMAINS=true
      - DHLEVEL=4096
      - TZ=Europe/London